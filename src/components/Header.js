// Step 1: Set up imports
import { element } from 'prop-types'
import React from 'react'

// Step 2: Create fat arrowfunction, name it file name
const Header = () => {

    // Step 4: must return a jsx element
    return (

        <header>
        App Component
        Header
        </header>
    )

}

// Step 3: Set up exports
export default Header